// Copy this file to environment.development.ts (gitignored) and fill in your token.
// Generate a token from the server virtualenv:
//   python -c "from app.utils.security import create_access_token; print(create_access_token({'sub': 'dev', 'role': 'user'}))"
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8000',
  devToken: 'PASTE_YOUR_SIGNED_JWT_HERE',
};
