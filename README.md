# MAPLE A1 Code Submission Evaluator

This is the MAPLE A1 Code Submission Evaluator module.

## Architecture

See `docs/design-doc.md` for the system architecture.

## Setup

1. Clone the repository.
2. Install dependencies: `pip install -r server/requirements.txt`.
3. Run the server: `uvicorn server.app.main:app --reload`.